{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.x",
  "regions": ["sin1"],
  "services": {
    "web": {
      "root": "apps/web",
      "framework": "nextjs",
      "installCommand": "cd ../.. && bun install",
      "buildCommand": "NEXT_PUBLIC_SERVER_URL=/api bun run build"
    },
    "server": {
      "root": "apps/server",
      "framework": "hono",
      "entrypoint": "server.mjs",
      "installCommand": "cd ../.. && bun install",
      "buildCommand": "bun run build && cp dist/index.mjs server.mjs",
      "routes": [
        {
          "src": "/api/((?!auth(?:/|$)).*)",
          "transforms": [
            {
              "type": "request.path",
              "op": "set",
              "args": "/$1"
            }
          ]
        }
      ]
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": {
        "service": "server"
      }
    },
    {
      "source": "/(.*)",
      "destination": {
        "service": "web"
      }
    }
  ]
}
